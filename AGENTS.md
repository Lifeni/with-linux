# AGENTS.md — with-linux

> 本文件是「工作流指令 ＋ 项目信息」。项目章程（目标 / 非目标 / 验收标准 / 约束 / 界面布局）已经用户逐节确认，任何改动需用户点头。

## 1. 项目信息

- **项目名**：`with-linux`
- **一句话目标**：跑在 arm64 开发板上、按需启动的终端工具箱（TUI）：顶部工具 tab 栏 ＋ 中间内容区 ＋ 底部状态提示，键盘优先、鼠标可用；第一个确定的工具是「OpenCode Go 用量查询」，后续要加什么、怎么加，到时候再说。
- **定位**：工具箱（不是只读、不是仪表盘），内容随用户需要增长。
- **扩展性**：机制不定、余地要留——框架与工具解耦，但不约定任何接口/注册机制。
- **技术栈**：Go ＋ Bubble Tea v2（样式 lipgloss，组件 bubbles）。
- **交付**：单一可执行文件，交叉编译 `linux/amd64` ＋ `linux/arm64`。
- **运行环境**：Debian arm64 开发板（本机即目标机），用户通过 SSH 使用；按需启动，**不常驻、不自启、不进 systemd**。
- **开发方式**：代码放在目标机上（本目录），本地终端只作显示层；改完直接跑，本地不留副本。
- **配置**：单个 JSON，`$XDG_CONFIG_HOME/with-linux/config.json`（默认 `~/.config/with-linux/config.json`），沿用已有 `apiKey` 字段名。
- **文档体系**：本文件 `AGENTS.md`；复杂流程用 `PLAN.md` / `FINDINGS.md` / `PROGRESS.md` 三件套。

## 2. 项目章程（用户已确认，2026-09-25）

### 非目标

**永不做（非目标清单）：**

1. 不支持 Linux 以外平台——只承诺 `linux/amd64`、`linux/arm64`，macOS/Windows 不编译不测试。
2. 不做多机/远程管理——只管本机。
3. 不做插件系统 / 远程加载第三方工具代码——工具只随主程序一起编译发布。

**v1 范围外（不是"永不做"，以后可能加，届时走范围扩张流程）：**

- 快捷文件跳转与编辑
- 日志查看
- 系统状态（CPU/内存/磁盘/温度）
- 一屏总览页（工具多了再议）

### 验收标准（EARS，v1 完成定义）

- **A1（骨架与交互）**：当用户在开发板的 SSH 终端里运行 `with-linux` 时，系统应当呈现「顶部工具 tab 栏（类似浏览器标签页）＋ 中间内容区 ＋ 底部状态提示」的 TUI，且鼠标点击顶部 tab、键盘快捷键选中，都能切换中间内容区；鼠标滚轮能滚动内容区。
- **A2（用量数据）**：当「OpenCode Go 用量」工具被选中且配置里的 `apiKey` 有效时，系统应当轮询 `GET https://opencode.ai/zen/go/v1/usage`，显示 rolling（5 小时窗）/ weekly / monthly 三个窗口的用量百分比与重置倒计时，展示格式（三列点阵进度 ＋ 倒计时文案）与已有零依赖面板一致。
- **A3（错误处理）**：当接口返回 401、403 或网络请求失败时，系统应当显示与旧实现一致的错误文案并按旧实现的重试策略继续轮询，TUI 不崩溃、不退出。
- **A4（配置）**：当配置文件不存在或 `apiKey` 为空时，系统应当在面板/状态栏提示缺配置及期望的配置文件路径，而不是崩溃或显示假数据。

### 约束

- 技术栈 Go ＋ Bubble Tea v2，样式 lipgloss，组件 bubbles。风险提示：bubbles 生态历史上跟随 v1，开工首件事做兼容性验证；有坑记录并暴露，**不换栈**。
- 框架与工具解耦，但不约定任何接口/注册机制（扩展性"余地要留"）。
- 单一可执行文件；交叉编译 `linux/amd64` ＋ `linux/arm64`。
- 不常驻、不自启、不进 systemd；SSH 终端使用。
- 数据获取：`GET https://opencode.ai/zen/go/v1/usage`，请求头 `Authorization: Bearer <apiKey>`。取数逻辑、401/403/网络失败文案、重试策略、三列点阵进度格式，实现前先只读调研旧零依赖面板源码并逐条对照移植。
- 轮询间隔：以旧实现为准（调研确认）；若旧实现未给出，则默认 60 秒且配置可改。

### 界面布局

```
┌──────────────────────────────────────────────────────────┐
│ with-linux   [ OpenCode Go 用量 ]   ← 顶部工具 tab 栏      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│   中间内容区（当前工具面板，占满剩余高度，可滚动）          │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ 当前工具 · 快捷键提示 · 错误/加载状态   ← 底部状态提示      │
└──────────────────────────────────────────────────────────┘
```

- 顶部 tab 栏：左端程序名 `with-linux`，右侧工具标签（v1 仅「OpenCode Go 用量」）。选中高亮、未选中暗色。鼠标点击切换；键盘 `←`/`→` 切换，`1`–`9` 直达。
- 中间内容区：当前工具面板，占满剩余高度，鼠标滚轮滚动；内容不足时不滚动。
- 底部状态提示：左侧当前工具名与快捷键提示（如 `q 退出 · ←→ 切换工具`），右侧加载/错误状态；配置缺失时提示配置文件路径。
- 全局：`q` 或 `Ctrl+C` 退出；终端 resize 自适应重排。
- **点阵变化无动画**（用户 2026-09-25 确认保持现状）：数据更新瞬时跳变，图案由确定性哈希生成、静止不抖动；每秒 tick 只更新倒计时文本。

## 3. 工作流指令（全程遵守）

1. **动手前先只读调研**（列目录、读文件、跑只读命令）；**破坏性操作前必须先问用户**。
2. **范围扩张的提议必须标注「这是范围扩张，需要用户确认」**，先写进章程再实现；非目标清单里的东西永不做。
3. **宣称「完成 / 修好 / 通过」之前必须跑验证并展示真实输出**，禁用「应该可以」「看起来对」。
4. **一次一个指令只改一件事**；改坏了回滚到上个可用版本，不在坏版本上连续修补。
5. **同一问题修 3 次没好，停下来质疑设计**，不要修第 4 次。
6. 复杂流程维护 `PLAN.md`（计划）/ `FINDINGS.md`（调研结论）/ `PROGRESS.md`（进度与验证记录）。
7. 面试/章程类决策以用户逐节确认为准，不脑补需求。
